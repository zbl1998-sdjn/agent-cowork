# 04 · 质量保障 · 交付发布 · 安全与隐私 — 实施计划

> 状态:讨论稿(可执行级)。是 v1.0 主计划(`00/01/02`)与体验增强(`03`)的"保证质量 + 安全交付"配套。
> 强约束:遵守 `00` 分层、`01` 工程标准、`02` 的 P0-T0 特征测试铁律。三部分:**Q 质量保障** · **R 交付发布** · **S 安全与隐私**。

---

## Q · 质量保障体系
### 设计要点
测试金字塔(下宽上窄):特征测试锁现有行为 → 单元(每模块)→ 集成(路由+沙箱+存储)→ 端到端(真实模型一次)→ 性能基准。**所有门禁汇成一条 `ci` 命令**,作为任务"完成"的前置。

| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 依赖 | 估 |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | 测试分层约定文档 + 目录/命名规范(*.test.js 同名邻近、e2e 单列) | `docs/testing.md` | 新增 | 五层职责清晰、命名统一 | — | S |
| Q2 | `check-arch`:扫 import 方向(L0→L4)+ 无环 | `scripts/check-arch.mjs` | 自带 fixtures 单测 | 违反分层/有环即失败 | — | M |
| Q3 | `check-filesize`:源文件软 250/硬 400 + 白名单(含理由) | `scripts/check-filesize.mjs` | 自带单测 | 超硬上限失败、软线告警 | — | S |
| Q4 | `ci` 一键:tsc/lint + check-arch + check-filesize + host/ui 测试(`--test-isolation=process`) | `scripts/ci.mjs` | — | 一条命令复现全量门禁、退出码可用 | Q2,Q3 | M |
| Q5 | 覆盖薄弱清单 + 补测(承接 P0-T0d) | `apps/host/test/*` `ui/src/**/*.test.*` | 即是测试 | 被拆/高危模块均有行为锁定 | P0-T0 | M |
| Q6 | 真实模型 E2E 冒烟脚本(每特性跑一次、留痕) | `scripts/e2e-smoke.mjs` | — | 关键流(聊天/读写文件/Shell)端到端绿、产出记录 | — | M |
| Q7 | 性能基准:启动/首屏/流式帧率/内存 基线 + 回归看护 | `scripts/bench.mjs` | — | 有基线数;回归超阈告警 | C5(03) | M |
| Q8 | 版本级验收门(DoD 之上):测试全绿 + 看护过 + 1 次 E2E + 安装版冒烟 | `docs/release-checklist.md` | — | 每个里程碑按此清单签发 | Q4,Q6,R5 | S |
| Q9 | host 类型护栏:`checkJs` + JSDoc + 渐进 `.ts` 转换门禁 | `tsconfig.host-checkjs.json` `scripts/check-host-types.mjs` `apps/host/src/**/*.{js,ts}` | 已覆盖 24 个 opt-in 模块,含 live artifact spec/viz、OAuth permissions、JSON store、tool registry;先 no-emit 类型检查,再逐批转换 | `npm run check:host-types` 可复现;接入 CI 后新增类型回归失败;不改变 host 运行栈 | P0-T10 | M |

---

## R · 交付与发布流程
### 设计要点
单机无远程也要"可复现 + 可回滚":每版 = tag + git bundle + 签名安装包归档。小步多次交付,每 1~2 特性出一个小版本。

| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 依赖 | 估 |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | `release` 一键:tag→git bundle→签名→归档 `releases/<ver>/`→写 VERSION | `scripts/release.mjs` | 干跑(dry-run)单测 | 一条命令产出可分发快照 | Q4 | M |
| R2 | 版本号(SemVer)+ CHANGELOG 约定(Keep a Changelog) | `CHANGELOG.md` | — | 每版有变更记录、号规范 | — | S |
| R3 | 自签 → 真 CA 证书(OV/EV)切换路径(脚本已支持 `-Pfx`) | `scripts/sign-windows.ps1` | — | 提供证书即可切真签名、文档化 | 决策 D2 | S |
| R4 | (P4)自动更新通道:Tauri updater + 签名校验 + 更新源 + 回滚 | `src-tauri/*` `scripts/*` | 更新流集成测 | 应用内完成一次更新、可回滚 | R1,R3 | M |
| R5 | 安装版冒烟脚本(装→host 自启→登录→真实回复→各面板) | `scripts/smoke-windows-client.ps1`(已有,补全) | — | 安装版关键路径自动冒烟 | — | M |
| R6 | 离线快照与可复现制度化(git bundle + 安装包归档进 `releases/`) | `scripts/release.mjs` | — | 任意历史版可还原 | R1 | S |

---

## S · 安全与隐私专项
### 威胁模型(谁→想干啥→怎么拦)
把已有控制项集中成一份"安全基线",每条配回归测试与里程碑复审;大量控制 v0.1 已落地,这里是**汇总 + 补测 + 持续看护**。

| ID | 威胁 / 控制 | 主要文件 | 前置/测试 | 验收 | 估 |
| --- | --- | --- | --- | --- | --- |
| S1 | 路径越权 → 工作区 jail + symlink/8.3/大小写 + 敏感段黑名单 | `security/path-policy.js` | 已有 path-policy 测,补边界用例 | 越权/敏感路径被拒;大小写不漏 | S |
| S2 | 副作用失控 → 审批门禁(mutating/high-risk),自动批准不覆盖高危 | `kimi/agent/*` `runtime/approvals` | 已有 approvals 测;子代理直接路由拒绝高风险/写入型工具,超上下文预算时 413 且不执行工具 | 高危必人工批准;拒绝可中止 | S |
| S3 | 密钥泄漏 → redaction + 不日志/回显/入库明文 + `.env`/`.AgentCowork` 忽略 | `security/redaction.js` `.gitignore` | 已有 redaction 测,加密钥扫描脚本 | 任何输出/记录不含明文 key | M |
| S4 | 网络滥用 → 本地后端不隔离(诚实+测试锁定);VM 后端真隔离 | `sandbox/*` | 已有(含 sandbox 网络诚实测) | 本地诚实报 false;VM `--network=none` 真隔离 | S |
| S5 | 鉴权/越权调用 → JWT/游客令牌 + 不信任客户端头 + CORS 白名单(含 `authorization`)+ 限流 + 体型限制 | `http/middleware/*` `auth/*` `routes/*` | 已有 cors/auth-gate/rate-limit/body-limit 测 | 无令牌 401、跨域受控、超额 429/413 | M |
| S6 | 主动内容上传 → 拒 `.exe/.bat/.ps1/.html/.svg` 等 | `workspace/file-operations.js` `routes/workspace-file-routes.js` | 已有上传白名单测 | 危险类型被拒 | S |
| S7 | 记忆隐私 → 本地存储、可查可删、显式确认学习 | `memory/profile.js`(03 B3) | 加"可删/不外发"断言 | 记忆全本地、可清除 | S |
| S8 | 连接器/MCP → 命令 allowlist + OAuth 凭证安全存储 + 每连接器权限可撤销 | `mcp/*` `connectors/*`(P2-B) | 已有 connector-allowlist、credential-store、connector-oauth、OAuth scope allowlist/单次审批 receipt、活页 connector-tool 数据源白名单与 `smoke:react-connectors` mock OAuth 闭环;补真实外部 OAuth | 仅白名单可执行;凭证不泄漏;权限可撤销 | M |
| S9 | 桌面外壳 → CSP `object-src 'none'` + 最小权限 capability + 安全 open_path | `src-tauri/tauri.conf.json` `capabilities/default.json` `src-tauri/src/*` | 已有 tauri-scaffold 测 | 无多余权限;open_path 限工作区 | S |
| S10 | 持续看护 → 每里程碑轻量安全复审 + 依赖漏洞扫描 | `scripts/audit-*.mjs`(已有 strict-audit) | — | 每版有复审记录;无高危依赖 | M |

---

## 排期建议
- **随 P0(地基期就要有)**:Q2/Q3/Q4(看护门禁)、Q5(补测)、Q9(host 类型护栏)、R1/R6(release 脚本+快照)、S3/S5(密钥扫描+鉴权回归汇总)。
- **随特性滚动**:Q6 每特性一次 E2E、S1/S2/S4/S6/S9 补回归。
- **随 P1/P2**:Q7 性能基准、S7 记忆隐私、S8 连接器安全。
- **随 P4**:R3/R4 真签名+自动更新、Q8 版本级验收门正式启用。

## 与其它计划的关系
- Q 把 `01` 的"测试规则"和 `02` 的 P0-T0/T1/T7/T10 操作化成脚本与门禁;R 把 `02` 的 P0-T8/P4 落成发布流程;S 把散在各处的安全控制汇成一份可回归、可复审的基线。
- 全部遵守 `00/01`:脚本与新模块小而专、面向接口、带测试、不进上帝类。
