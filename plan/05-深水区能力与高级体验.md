# 05 · 深水区能力与高级体验 — 实施计划

> 状态:讨论稿(可执行级)。专放 `02/03` 之外的**增量**能力与体验(不与已规划重叠)。遵守 `00` 分层 / `01` 标准 / P0-T0 特征测试铁律 / `04` 的质量门禁。
> 两大块:**A 功能能力(深水区)6 项** · **B 高级体验 6 项**。每项拆成细任务。

---

## 0. 模块归属与新增目录(对齐 `00` 的 L0→L4 + 可插拔接口)
| 能力 | 层 | 新增/改动模块 | 接口(插件化) |
| --- | --- | --- | --- |
| 本地 RAG 检索 | host L1 | `workspace/index/{chunk,store,retriever}.js` + 工具 `SearchWorkspace` | `WorkspaceRetriever{ index(root), search(q)->{chunks,sources} }` |
| 办公动词 | host L1 | `tools/office/{xlsx,pdf,docx,pptx,image}.js`(或经 skills) | 各 `OfficeOp` 工具,统一审批/jail |
| 对话固化成技能 | host L1 | `recipes/capture.js` | `captureRun(runId)->recipe` |
| 数据分析闭环 | host L1 | `tools/data/{profile,analyze,report,artifact}.js` | `DataTool` |
| 开发者模式 | host L1 + UI | 复用 Edit/Shell + `tools/dev/git.js` + "开发者模式"预设 | `GitOp` |
| 网页自动化 | host L1 | `tools/browser/*`(或浏览器连接器,接 P2-B) | `BrowserTool` |
| 对话分支/编辑 | host 存储 + UI | `storage/conversation-store`(消息树/分支)+ `hooks/useConversations` | 分支 API |
| 拖拽/右键/快捷键 | UI | `hooks/useShortcuts.ts` `components/ui/ContextMenu.tsx` + DnD | 快捷键/命令注册表 |
| 富产物预览/编辑 | UI | 扩 `components/FilePreview.tsx` `PreviewCard.tsx`(表格/图片/PDF/diff) | — |
| 项目工作区 | host 存储 + UI | `storage/projects.js` + `memory` 项目层 + `components/panels/ProjectsPanel.tsx` | `ProjectStore` |
| 可解释/成本透明 | UI + host | `runtime/usage` + `components/chat/ToolCallCard.tsx` `SourcesFooter.tsx` | — |
| 长任务通知/后台 | host + 外壳 | `runtime/background-tasks.js` + notification 插件 | `BackgroundTask` |

> 全部做成独立小模块 + 接口注入,禁止塞进现有上帝类;碰存储/工具/产物等现有模块前,先按 P0-T0 补特征测试。

---

## A · 功能能力(深水区)
### A1 本地 RAG 工作区检索(差异化大杀器)
设计:可插拔检索器(先关键词/BM25 + 分块,嵌入式可选);索引仅限工作区 jail;回答带来源引用。
> **状态(2026-05):关键词版已完成且测试全绿**(`workspace/index/{chunk,store,retriever}.js`,零外部依赖)。**嵌入/向量进阶版(A1d)已提前留位、延后——卡外部依赖(嵌入模型/向量库),用户暂无法提供。** v1.0 出货走关键词版即可;接口已按可插拔设计,后续接入嵌入只换实现、不动调用方。
>
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| A1a | 分块 + 索引存储(增量更新、限工作区) | `workspace/index/{chunk,store}.js` | 新模块单测 | 建/增量索引正确、jail 内 | M | ✅ 已完成 |
| A1b | 检索器接口 + 关键词实现 + 来源引用 | `workspace/index/retriever.js` | 新模块单测 | 召回相关块、附文件/行来源 | M | ✅ 已完成 |
| A1c | `SearchWorkspace` 工具 + 路由 + 前端来源展示 | `tools/*` `routes/search-routes.js` `SourcesFooter.tsx` | 工具+路由测 | "在我的资料里找/答"端到端、带来源 | M | ◐ 关键词路径基本就绪 |
| A1d | 嵌入式检索升级(向量召回 + 重排) | `workspace/index/embedding-retriever.js`(换实现) | 接入嵌入能力后补测 | 语义召回优于关键词、来源仍准确 | M | ⏸ **延后·卡外部依赖**(待用户提供/接入嵌入模型或向量库后再排) |

### A2 办公动词深化
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 |
| --- | --- | --- | --- | --- | --- |
| A2a | Excel 深操作(公式/透视/图表) | `tools/office/xlsx.js`(或 skill) | 单测 + 样例文件 | 生成/改写 xlsx 正确可打开 | M |
| A2b | PDF 合并/拆分/填表/(签名) | `tools/office/pdf.js` | 单测 | 关键 PDF 操作可用 | M |
| A2c | Word/PPT 模板化 + 图片裁剪/格式/**OCR** | `tools/office/{docx,pptx,image}.js` | 单测 | 模板生成 + OCR 可用 | L |
| A2d | 批量文件整理/去重/改名(审批+回滚) | `workspace/file-operations.js` | 先补 file-operations 特征测试 | 批量操作有预览/审批/回滚 | M |

> **状态(2026-05):A2 的本地文档产物切片已落地一段。** `summary-report` 已能生成 DOCX/PPTX/PDF + Markdown,产物 catalog 能识别 Word/演示/表格/PDF 类型;已有 XLSX writer 继续服务表格类 recipe。真实 Office 应用打开、OCR、PDF 深操作和公式/透视/图表仍未计作完成,按 A2a/A2b/A2c 后续拆小批推进。

### A3 把成功对话固化成技能
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 |
| --- | --- | --- | --- | --- | --- |
| A3a | ✅ `captureRun(runId)->recipe`(抽参/脱敏) + 后端草稿路由 | `recipes/capture.js` `routes/recipe-routes.js` | recipes/route 特征测试 | 一次成功对话存成可复用技能草稿 | M |
| A3b | ◐ UI"存为技能"+ 技能管理 | `components/panels/*` `lib/api/recipes.ts` | 组件抽测 | 可存/编辑/运行自定义技能 | M |

A3a 完成记录(2026-05-27):新增 `POST /api/recipes/capture`,从当前租户作用域内的 `runsIndex.get(runId,{ tenantId })` 读取 run record 并调用 `captureRun` 生成脱敏技能草稿;路由不走裸 `runStoreRoot` 兜底,避免跨租户/路径绕读。新增 route 集成测试覆盖成功固化、跨租户 404、非法 runId 400 和 secret 脱敏。A3b 的 UI 存为技能、自定义技能持久化/编辑/运行仍留下一小切片。

A3b 保存切片完成记录(2026-05-27):完成消息级"存为技能"按钮、`/api/recipes/capture` 前端 API 封装、保存中/已存草稿/失败状态和脱敏草稿回显卡片;新增租户隔离的 custom recipe store 与 `POST /api/recipes/custom`,只接受已脱敏草稿,`GET /api/recipes` 合并当前租户自定义 recipe,自定义 recipe 可复用现有审批型 run 路径生成 Markdown 产物。Timeline/route 集成测试覆盖保存入口、草稿摘要、跨租户隐藏、运行自定义 recipe 和未脱敏拒绝。编辑表单、技能管理列表和删除/重命名仍未完成。

### A4 数据分析闭环
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| A4a | CSV/Excel 剖析→统计→图表→报告 | `tools/data/*`(接 data skills) | 单测 + 样例 | "给我分析这个表"出图+报告 | M | ✅ CSV/TSV/XLSX + 图表 artifact 本地闭环 |

A4a CSV/TSV-first 完成记录(2026-05-26):新增 `tools/data/report.js` 组合现有 `profileDataFile`/`readDataTable`,输出 `data-analysis` 结构、聚合图表 spec 和 Markdown 报告草稿;新增 builtin `data.analyze`,并把 agent `AnalyzeDataFile` 升级为"列统计→图表数据→报告草稿"的只读安全工具。路径继续复用 trustedRoot jail、文件大小/行数限制和敏感路径策略;工具保持 `mutating:false`/`risk:safe`,不写 artifact、不安装 pandas/numpy/matplotlib、不联网。已补 host 单测覆盖 CSV 聚合图表、Markdown 报告、builtin/agent 暴露,并把 `tools/data/profile.js`/`report.js` 纳入 host `checkJs` 类型护栏。真实图表 artifact 生成和数据科学重依赖仍留后续小切片。

A4a XLSX 读取完成记录(2026-05-27):`readDataTable` 已支持 trustedRoot 内 `.xlsx` 首个 worksheet,复用 ZIP 读取的 entry 数/单 entry/总解压大小限制,支持 inline/shared string 与缓存值,不执行公式、不写 artifact、不联网。`data.profile`/`data.analyze`/`AnalyzeDataFile` 描述同步扩展到 CSV/TSV/XLSX,新增 XLSX 图表分析单测并纳入 host `checkJs`。Excel 多 sheet/公式语义和 pandas/numpy/matplotlib 增强仍留后续小切片。

A4a 图表 artifact 完成记录(2026-05-27):新增 `tools/data/artifact.js` 与审批型 `data.createChartArtifact`/`CreateDataChartArtifact`,复用 `analyzeDataFile` 的推荐 chart spec 并通过现有 `buildLiveArtifact` 写入 trustedRoot 下 `.AgentCowork/artifacts/*.html/.json`;工具显式 `mutating:true`、`risk:high`、`requiresApproval:true`,直接工具路由会拒绝无 agent 审批调用。host 单测覆盖 builtin/agent 描述符、artifact manifest、相对路径和 chart 数据,并纳入 host `checkJs`。真实 pandas/numpy/matplotlib 增强仍留后续切片。

### A5 开发者模式增强
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 |
| A5a | git 操作工具(status/diff/commit,审批) | `tools/dev/git.js` | 单测 | 常用 git 经审批可用 | M |
| A5b | "开发者模式"预设(多文件编辑/跑测试脚手架) | `kimi/system-prompt.js` + 预设 | 提示快照测 | 开发场景顺手 | S |

### A6 网页自动化
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 |
| A6a | 浏览器工具/连接器(抓取+交互),沿用连接器权限 | `tools/browser/*` 或 MCP(P2-B) | 单测/契约测 | 能完成"上网帮我办"类任务、权限可控 | L |

---

## B · 高级体验
| ID | 任务 | 主要文件 | 前置/测试 | 验收 | 估 |
| --- | --- | --- | --- | --- | --- |
| B1a | 对话消息树/分支模型(改历史消息→分支) | `storage/conversation-store.js` | 先补 conversations 特征测试 | 编辑历史生成分支、不破坏原线 | M |
| B1b | 分支 UI:切换/对比/回到节点 | `hooks/useConversations.ts` `components/*` | 组件纯逻辑测 + `smoke:react-branches` | 可视化分支、可回溯;真实浏览器主线→分支→回主线已覆盖 | M |
| B2 | 拖拽(文件入/产物出)+ 右键菜单 + 统一快捷键体系 | `hooks/useShortcuts.ts` `components/ui/ContextMenu.tsx` | 抽逻辑测 | 拖拽/右键/快捷键可用、可发现 | M |
| B3 | 富产物预览/编辑(表格/图片/PDF/代码 diff 内联) | `components/FilePreview.tsx` `PreviewCard.tsx` | 抽逻辑测 | 多类型内联预览 + 轻编辑 | L |
| B4 | 项目工作区(对话/记忆/产物按项目组织) | `storage/projects.js` `memory` 项目层 `components/panels/ProjectsPanel.tsx` | 先补 store 特征测试 | 项目级上下文/切换 | L |
| B5 | 可解释 + 成本透明(工具"为什么"+ 来源跳转 + token/费用) | `runtime/usage` `ToolCallCard.tsx` `SourcesFooter.tsx` | 计费聚合 + 组件测 | 可看到原因/来源/用量费用 | M |
| B6 | 长任务系统通知 + 后台继续运行 | `runtime/background-tasks.js` + notification 插件 | 单测 | 任务完成有通知、可后台跑 | M |

---

## 排期建议(在主线 P1/P2 之上滚动)
- **第一梯队(差异化最大)**:~~A1 本地 RAG(关键词版已完成)~~、A2a/A2d 办公动词、B1 对话分支、B3 富产物预览。(A1 嵌入进阶版 A1d 已留位延后,不占本梯队)
- **第二梯队**:A3 技能固化、A4 数据分析、B4 项目工作区、B5 可解释/成本。
- **第三梯队**:A5 开发者模式、A6 网页自动化、B2 拖拽/快捷键、B6 后台通知。
- 仍按"小步多次交付":每 1~2 条出一个小版本;每条满足 `01` DoD + `04` 门禁。

## 风险与开放问题
- **RAG**:✅ 已决——**关键词版(零依赖)已完成上线;嵌入式进阶版(A1d)延后,卡外部依赖(嵌入模型/向量库),待用户具备条件再排。** 接口已可插拔,升级时只换实现。
- **OCR / 浏览器自动化**:需外部能力(OCR 引擎 / 浏览器),按连接器接入、权限受控。
- **对话分支/项目工作区**:改存储 schema,务必先特征测试 + 迁移脚本(对接 `04` 数据/迁移)。
- 待你拍板:A6 网页自动化走"内置工具"还是"浏览器连接器(MCP)"。

> ⏸ **提前留位 · 卡外部依赖(本计划集统一登记)**:① RAG 嵌入式进阶版(A1d)——待嵌入模型/向量库;② 真 CA 代码签名(见 `02` P4-A)——待证书。两项均不阻塞 v1.0 主线,具备条件后即可单独排期。
