# 02 · v1.0 任务拆解(细颗粒 · 按特性小步交付)

> 交付节奏:**每个任务尽量是一个独立可合并的小改动**(带测试),完成即可单独验证。估算:S≈半天内 / M≈1~2 天 / L≈需再拆。每个任务都受 `00`(依赖)+`01`(标准)约束。
>
> 决策已锁(见 `README.md` D1–D4,可复议):Windows-only(跨平台→v1.1)/ 暂自签+文档 / 先读图+文档图表(图像生成待定)/ 不做遥测。

## P0 · 地基与拆分(无新功能、只降风险,先做 → 出 v0.2.0)

### P0-T0 · 重构前安全网(characterization tests · 最先做,先于一切拆分)
> 原则:**拆任何文件前,先用测试把它"当前的行为"钉死;拆分过程中这些断言一律不改**,一改坏立刻红、可立即回滚。盘点显示 host 覆盖已强,缺口集中在前端与 agent-runner。
>
> | ID | 任务 | 覆盖现状 | 验收 | 估 |
> | --- | --- | --- | --- | --- |
> | P0-T0a | 前端 `api.ts` 特征测试:URL 解析、Bearer 注入、请求体成形、SSE 解析、错误/超时处理、hostReady 探活 | **零覆盖(最大风险)** | 关键路径有断言;拆分后不改断言仍全绿 | M |
> | P0-T0b | 前端 `App.tsx`:把纯逻辑抽成可测函数(convTitle、消息归约、进度图标映射、chatEnabled 自愈分支)并补测 | 零覆盖 | 纯逻辑有单测;拆 hooks/子组件后行为不变 | M |
> | P0-T0c | host `agent-runner` 直接特征测试:工具循环、审批门禁、no-reply 兜底、usage 累计、取消 | 仅间接覆盖 | 拆成 4 个子模块后断言不改仍全绿 | M |
> | P0-T0d | 复核 `recipes/registry`、`sandbox/code-runner`、`artifacts/live-artifact`、`cli-runner` 覆盖,薄处补齐 | 部分间接 | 各被拆模块拆前都有行为锁定测试 | S |
>
> 完成 P0-T0 后才进入下方 P0-T2/T3/T4/T5/T6 的拆分;拆分任务的"验收"统一加一条:**P0-T0 对应的特征测试零改动仍全绿**。

| ID | 任务 | 主要文件 | 验收 | 依赖 | 估 |
| --- | --- | --- | --- | --- | --- |
| P0-T1 | 架构看护脚本:import 方向/无环 + 文件体积 | `scripts/check-arch.mjs` `scripts/check-filesize.mjs` | 违反分层/超硬上限即失败;接入 test 前置 | — | S |
| P0-T2 | 拆 `server.js`:中间件(cors/auth/rate-limit/headers/static)+ 路由挂载 | `http/middleware/*` `routes/*` `server.js` | server.js<200 行;全量 host 测试不变全绿 | T1 | L |
| P0-T3 | 拆 `api.ts` 按域 | `lib/api/{transport,auth,chat,files,recipes,schedules,kimiConfig}.ts` | 每文件<200 行;ui 测试全绿;行为不变 | — | M |
| P0-T4 | 拆 `App.tsx`:抽 hooks + 子组件 | `hooks/useAgentStream.ts` `useConversations.ts` `useKimiInfo.ts` + `App.tsx` | App.tsx<250 行;交互不变 | T3 | L |
| P0-T5 | 拆 `agent-runner.js` | `kimi/agent/{tool-loop,approval-gate,finalize,toolset-builder}.js` | 主循环<250 行;agent 测试全绿(含 no-reply 兜底) | — | M |
| P0-T6 | 拆 `memory-store.js` | `memory/{store,layers,query}.js` | 每文件<250 行;memory 测试全绿 | — | M |
| P0-T7 | 测试隔离稳定 + 一键 CI | 修 conversations/exec-child 等;`scripts/ci.mjs` | `--test-isolation=process` 全绿且可复现 | — | M |
| P0-T8 | release 脚本化 | `scripts/release.mjs` | 一条命令:tag→bundle→sign→归档到 releases/ | T7 | S |
| P0-T9 | 模型 provider 抽象 seam | `kimi/provider/{index,kimi}.js`(只重构) | 调用方只依赖接口;现有 kimi 行为不变 | — | M |
| P0-T10 | host 渐进式 TypeScript 迁移:先 `checkJs` + JSDoc,再逐文件转 `.ts` | `tsconfig.host-checkjs.json` `scripts/check-host-types.mjs` `apps/host/src/**/*.{js,ts}` | 不改 Node host 语言栈、不重写;首阶段只做 no-emit 类型检查与公共接口 JSDoc,后续从 L0/L1 叶子模块小批量转 `.ts`;每批 host 测试与 `npm run check` 全绿 | T1,T2,T5,T6 | L |

### P0-T10 · host 渐进式 TypeScript 迁移(新增)
目标不是重写 host,而是在已拆成小模块的边界上加类型护栏,降低跨模块参数/返回值回归。

| 子项 | 任务 | 验收 |
| --- | --- | --- |
| T10a | 建 `checkJs` 基线:`allowJs/checkJs/noEmit` + JSDoc 标注关键公共面,先覆盖 opt-in 模块清单 | `npm run check:host-types` 可本地复现;发现真实类型问题但不要求一次吃完整个 host |
| T10b | 接入质量门禁:类型检查进入 `npm run check`/CI,允许短期白名单但必须写理由和到期条件 | 新增类型回归会红;白名单不吞掉新增文件 |
| T10c | 逐文件转 `.ts`:从纯函数、L0 安全/HTTP 工具、L1 叶子模块开始,每批不超过 3~5 个模块 | 运行行为不变;导入路径/打包路径清晰;host 测试进程隔离全绿 |
| T10d | 扩到路由/运行时/agent 核心前,先把对应接口类型和特征测试补齐 | 不用类型迁移顺手改架构或语义;每个转换 PR 都是小而可回滚 |

T10a/T10b 扩展记录(2026-05-25):Host 类型护栏从 19 个 opt-in 模块扩到 25 个,新增纳入 `artifacts/live-spec.js`、`artifacts/viz.js`、`artifacts/office-writers.js`、`connectors/oauth-permissions.js`、`storage/json-store.js`、`tools/tool-registry.js`,补齐对应 JSDoc 与 Node shim 的 `crypto.randomUUID`/`fs.renameSync`/`Buffer`/`zlib` 声明,并继续由 `npm run check:host-types`/`npm run check` no-emit 门禁看护。直接 `.ts` 转换仍需先明确当前直接 Node 运行模式下的导入/打包路径,不得为了迁移改语言栈或重写运行链路。

## P1 · 能力深化(价值核心,逐特性出版本)
### P1-A 计划-执行-自检闭环 + 可见 Todo
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P1-A1 | 后端 todo 状态机(计划项→进行中→完成/失败)经 SSE 推送 | 事件流含 todo 更新;单测覆盖状态流转 | M |
| P1-A2 | 前端 Todo 进度组件(实时勾选)接入消息流 | 多步任务里 Todo 实时更新 | M |
| P1-A3 | 计划模式增强:研究→计划→批准→执行→自检→收尾 串联 | 一个多文件重构任务走完闭环,自检通过;端到端实跑留档 | M |

### P1-B 子代理并行
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P1-B1 | 子代理接口 + 上下文预算/审批隔离 | 子代理独立上下文与审批;单测 | M |
| P1-B2 | 主代理 拆分-派发-并行-汇总(并发上限可配) | "并行分析 3 个文件夹并汇总"比串行明显快、结果正确 | L |
| P1-B3 | UI:子任务分组展示 | 子任务进度分组清晰 | S |

P1-B1 本地可测子项完成记录(2026-05-25):已有 `/api/subagent/run` 顺序执行接口、`subagent-run` 记录与时间线;本批新增子代理独立上下文预算与步数上限,超预算计划返回 413 且不会执行任何工具。审批隔离保持:直接子代理/工具路由拒绝高风险、写入型或 `requiresApproval` 工具,这类操作仍只能走 agent 审批流。

P1-B2 代码+单测完成记录(2026-05-25):新增 `/api/subagent/parallel` 并行路由和主 agent `AgentParallel` 工具,支持可配并发上限、子任务并发执行、聚合 `subagent-parallel-run` 记录、子 run 汇总和摘要返回;所有子任务会在启动前统一校验审批风险、工具描述符与上下文预算,任一子任务超预算或包含高风险/写入型工具时不会启动任何子代理。已用 host 单测覆盖并发比串行更快、聚合 run 落盘、预算前置拒绝、直接路由审批隔离和 agent tool 并发汇总;`npm run test:host` 通过(473 tests,472 pass,1 skip)。真实"并行分析 3 个文件夹并汇总"端到端性能留到 P1-B 后续验收。

P1-B3 代码+单测完成记录(2026-05-25):`AgentParallel` 向执行流发出 `child_start/child_end` 生命周期事件;前端 reducer 会把子任务按 index 合并为分组状态,时间线中展示目标、进行中/完成/失败、步数和错误摘要。已用 host 单测覆盖子任务事件发射,UI 单测覆盖事件归约与 `SubtaskGroups` 静态渲染;`npm run test:host` 通过(473 tests,472 pass,1 skip),`npm run test:ui` 通过(14 files,65 tests)。安装版/WebView 视觉深验留到 04-R5 后续验收。

### P1-C 多模态(先做文档/图表,不依赖图像生成)
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P1-C1 | 文档产物技能 docx/xlsx/pptx/pdf + 产物面板一键打开 | "要点→一页 Word""数据→Excel"可用 | M |
| P1-C2 | 读图链路补齐(贴图→理解→据图操作) | "表格截图→整理成 Excel"端到端可用 | M |
| P1-C3 | (待定)图像生成 provider | 开放问题③确认后再排 | M |

P1-C1 代码+单测完成记录(2026-05-25):`summary-report` recipe 在原 Markdown 输出外新增 DOCX、PPTX、PDF 产物,复用 trusted root 下的 recipe 输出路径与现有 artifact catalog/open 链路;DOCX/PPTX 使用本地 OOXML ZIP writer,PDF 使用本地轻量 writer,现有 XLSX writer 仍覆盖 meeting-actions/excel-cleaning 等表格产物。已用 host 单测覆盖 Office writer 内容/转义、recipe 多产物输出与产物类型标记,`npm run test:host` 通过(478 tests,477 pass,1 skip),`npm run check` 通过。真实 Office 应用打开、OCR 和安装版 WebView 深交互不计入本条完成,留到延期验收。

### P1-D 审批/进度/错误 UX 打磨
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P1-D1 | 批量批准 + 记住本会话同类操作 | 多审批任务点一次覆盖同类 | S |
| P1-D2 | 工具卡片信息增强(参数/结果/失败原因/耗时) | 卡片可读、失败有因 | S |
| P1-D3 | 取消 / "继续" + 明确失败态/空态文案 | 任意运行都有清晰收尾 | S |

P1-D1 代码+单测完成记录(2026-05-26):Host approval registry 与 Postgres approval store 新增 `resolveMany(ids, decision, context)`,并新增 `POST /api/approvals/batch` exact-ID 批量 resolve 路由。该路由只接受非空、去重、格式合法的显式 approval id 列表,逐个复用原有 tenant/user scope 与 decision 归一化,不支持 runId/wildcard/all,也不引入按工具名通配自动批准。已补 in-memory、PG 跨实例、HTTP 路由、非法 id 与异步 store 单测;前端当前仍保持单 approval 按钮,批量 UI 入口留后续 UX 细化。

P1-D2 代码+单测完成记录(2026-05-26):Host `tool_result` 事件补充 `durationMs`,前端 `agentChatStream` 透传耗时元数据;聊天时间线的工具调用卡片展示状态、耗时与失败原因,展开后分区显示参数和结果。已补 host 单测锁定工具结果耗时事件,补 UI 单测锁定失败原因/耗时渲染;真实长任务视觉验收留到 P1-D 整体验收。

P1-D3 代码+单测完成记录(2026-05-26):前端 `agentChatStream` 将 `cancelled` SSE 从 `done` 中拆出并透传给 UI;停止运行后消息进入 `cancelled` 状态,保留已有输出并展示可继续的收尾文案。失败/取消的 assistant turn 现在显示"继续"入口,任务状态徽标补齐"已取消"标签。已补 UI 单测覆盖取消帧路由、失败/取消继续入口和取消态徽标;后端断点续跑 HTTP 入口与真实长任务视觉验收留到后续 `07-D`/P1-D 整体验收。

## FE · 前端优化专项线(本轮聚焦:交互修复 / 架构分层+错误空态 / 流式性能;设计系统暂缓)
> 现状盘点:23 组件平铺、单一 `styles.css`、几乎无 memo、无懒加载/虚拟化/拆包、`App.tsx:237` 无条件滚到底。

### FE-1 交互修复(快赢,可早做)
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| FE-1a | 智能跟随滚动:抽 `useStickToBottom` hook——仅当用户已贴底才随流自动滚;往上翻历史不被拽回 | 翻看历史不被打断;贴底时正常跟流 | S |
| FE-1b | "回到底部 ↓" 按钮:不在底部且有新内容时出现,点击平滑回底 | 离底有按钮、点击回底 | S |

### FE-2 组件架构分层(与 P0-T4 拆 App.tsx 衔接)
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| FE-2a | `components/` 分层:`ui/`(原语 Button/Input/Modal/Drawer/Card/IconButton)、`chat/`、`panels/`,迁移现有 23 组件归类 | 目录分层清晰、导入统一、行为不变 | M |
| FE-2b | 抽公共 UI 原语,替换散落的内联按钮/输入/弹层 | 原语复用、样式一致 | M |

### FE-3 错误/空/加载/失败态(前端版"永不空交代")
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| FE-3a | `ErrorBoundary` 包裹主区与各面板 | 任一区块出错不白屏,显示可读错误 + 重试 | S |
| FE-3b | 抽 `<Empty>/<Loading>/<ErrorState>` 复用组件,替换内联态 | 空/加载/失败态统一可读 | M |

### FE-4/5/6 流式渲染性能
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| FE-4 | 流式局部化:token 更新只重渲当前气泡;`memo` 化列表项 + `useCallback` 稳定回调 | 长对话流式输出不卡;重渲范围收敛(profiling 佐证) | M |
| FE-5 | 长会话虚拟化(零依赖倾向,优先自实现轻量 windowing) | 数百条消息滚动流畅 | M |
| FE-6 | 面板 `React.lazy` 懒加载 + vite `manualChunks` 拆包(react/recharts/markdown) | 首屏 JS 更小、面板按需加载 | S |

> 暂缓:**设计系统**(设计令牌 + 拆 `styles.css` + 全面视觉看齐)留作后续专项;FE-2a 的 `ui/` 原语先把结构立住,样式令牌化下一轮再做。

## P2 · 安全 + 连接器
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P2-A1 | 启动探测 Docker/WSL,可用则默认 VM 后端 `--network=none` | 有 WSL/Docker 时默认真隔离 | M |
| P2-A2 | 不可用回退本地 + UI 明确提示"本地不隔离网络" | 回退路径有提示 | S |
| P2-A3 | 集成测试:隔离模式下联网被真实阻断 | 测试通过 | S |
| P2-B1 | 连接器安装/管理面板 + 精选注册表 | 面板一键装并调用 | M |
| P2-B2 | OAuth 连接器打样(GitHub/Notion/邮箱择一)+ 凭证安全存储 | 一个真实 OAuth 连接器跑通 | L |
| P2-B3 | 每连接器细粒度权限 + 审批 + 可撤销 | 权限可控可撤销 | M |

P2-A 完成记录(2026-05-25):Host 启动探测 Docker/WSL;Docker daemon + `KCW_SANDBOX_DOCKER_IMAGE` 指向本地已有镜像时默认选 `vm:docker` 并使用 `--network=none`;不可用时回退 local,并在 `/api/sandbox/info` 与设置页自检显示"本地不隔离网络"。新增 gated 真实 Docker 集成测试,本机以 `KCW_SANDBOX_REAL_DOCKER_IMAGE=postgres:16-alpine` 通过联网阻断验收。

P2-B1 完成记录(2026-05-25):连接器精选 catalog、`/api/connectors`、`/api/connectors/connect` 与侧栏管理面板已覆盖;新增 `smoke:react-connectors` 真实浏览器验收,确认面板一键连接内置 filesystem MCP 后 `mcp__fs__read_text` 进入工具 registry。安全边界保持:连接端点只接受 host-defined connector id,拒绝客户端传入 command。

P2-B2 本地可测子项完成记录(2026-05-25):新增 GitHub OAuth device-flow connector prototype、`/api/connectors/oauth/{status,start,complete,revoke}`、服务端 device-code session 与 Host 凭证仓库;Windows 默认 DPAPI 保护 access token,前端只拿到 user code/verification URL/session id,状态/撤销只返回脱敏摘要。新增 host 单测覆盖凭证落盘脱敏、token 取回、撤销、OAuth pending 与完成授权;新增 UI API 单测和 `smoke:react-connectors` mock GitHub device-flow 真实浏览器闭环。真实 GitHub 账号授权仍需外部 OAuth App client id 与人工浏览器授权,暂不计作真实外部 OAuth 验收。

P2-B3 本地可测子项完成记录(2026-05-25):新增 host-defined connector 断开/撤销路径,filesystem MCP 断开后关闭 client 并移除 `mcp__fs__*` 工具;OAuth 连接器新增 allowlist scope catalog、`/api/connectors/oauth/approve` 单次 approval receipt、审批缺失/重放/越权 scope 拒绝和高风险 scope 标记;前端面板支持逐 scope 勾选、先审批再授权、撤销后清空审批态。`smoke:react-connectors` 覆盖连接→断开→工具撤销→GitHub OAuth scope 审批→授权→撤销;真实 GitHub 账号授权仍归 P2-B2 外部 OAuth 延后项。

## P3 · 多模型(可与 P1/P2 并行,基于 P0-T9)
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P3-A | 接入 OpenAI / Claude / 本地 OpenAI 兼容 provider | 同任务可切 2 家跑通 | M |
| P3-B | 路由 + 失败降级链(复用 circuit-breaker) | 某家挂了自动降级 | M |
| P3-C | 用量/成本面板(token/费用,按 provider) | 面板显示用量与费用 | M |
| P3-D | 每会话切模型 + 统一 BYO-key 设置 | 会话级切换生效 | S |

## P4 · 可分发(功能稳定后)
| ID | 任务 | 验收 | 估 |
| --- | --- | --- | --- |
| P4-A | ⏸ **延后·卡外部依赖(待证书)** 真 CA 代码签名;在此之前维持自签 + 文档(决策 D2),不阻塞主线 | 拿到证书后:真签名包无 SmartScreen | S |
| P4-B | 自动更新(Tauri updater + 签名校验) | 应用内完成一次更新 | M |
| P4-C | 跨平台 macOS/Linux(建议 v1.1) | 至少再跑通 1 平台 | L |
| P4-D | 安装包瘦身 + 首次运行引导 | 包更小、首启有引导 | S |

## 里程碑与节奏
- **v0.2.0** = 全部 P0(地基/拆分,行为不变、全绿)。之后每完成 1~2 个特性任务就出一个小版本给你试。
- 每个任务遵循 `01` 的 DoD 清单;每个里程碑按 `01-F` 打 tag + 出快照。

## 建议的执行顺序(前 6 刀)
1. P0-T1(看护脚本)→ 2. P0-T2(拆 server.js)→ 3. P0-T3(拆 api.ts)→ 4. P0-T5(拆 agent-runner)→ 5. P0-T7(测试隔离)→ 6. P0-T10a(host `checkJs`+JSDoc 类型护栏)→ 7. P1-A1+A2(计划闭环+Todo,先让你看到"能力"上的新东西)。
