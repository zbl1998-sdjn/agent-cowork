# 端到端验证报告 · 2026-06-02

> 触发:JS→TS 全量重构后,按项目既有标准对**整个项目**做一次端到端验证(非仅验改动 diff)。
> 分支:`docs/zh-core-comments`(HEAD `df99794`),对比 `main`:943 文件、+47648/−28639;`apps/host/src` 现 0 `.js` / 274 `.ts`,`scripts` 0 `.js` / 51 `.ts`。
> 标准来源:`scripts/ci-gates.ts`(`npm run ci` = check → test:host → test:ui →〔eval〕)、`docs/release-checklist.md`、`docs/testing.md`。

## 结论

**整项目通过既有标准门禁。** 唯一非绿项 `eval`(replay 模式)为**输入缺失型阻断**,非代码/重构缺陷,且项目自身的本地门禁在缺 records 时本就跳过它;已用官方离线契约 dry-run 证明 eval 管线可用(28/28)。

> **复验补记(2026-06-02T07:32–07:54 UTC):** 首轮证据(03-02 UTC)之后,工作区又做了配置清理(`eslint.config.ts` / `package.json` lint glob / `tsconfig.host-test.json` / `app-run-events.ts`,改于本地 13:26–13:35),晚于首轮完整门禁日志(本地 13:02)。已在**当前最终工作区**重跑完整权威门禁(16 步,见 §八.1),静态门禁 + 测试 + smoke + 构建逐项复绿;并补齐**全部三项**原"待验收"——**真实 Kimi 模型回复**(§八.2)、**已安装 exe 真实启动**(§八.3)与**真实浏览器渲染**(§八.4),均通过并落盘证据。**至此已无遗留待验收项。**

## 一、静态门禁(整代码库) — 17/17 ✅

并行逐项执行 `npm run check` 的全部子门禁(覆盖 **765 源文件 / 10 个 tsconfig 工程**),全部 `exit 0`:

| 门禁 | 结果 | 门禁 | 结果 | 门禁 | 结果 |
|---|---|---|---|---|---|
| check:arch | ✅ | check:script-types | ✅ | check:resource-js | ✅ |
| check:filesize | ✅ | check:bootstrap-js | ✅ | check:js-boundary | ✅ |
| check:secrets | ✅ | check:resource-types | ✅ | check:ts-coverage | ✅ |
| check:host-types | ✅ | check:ui-types | ✅ | check:ts-language-service | ✅ |
| check:eval-types | ✅ | check:lint(ts+resources-js) | ✅ | check:icons | ✅ |
| check:host-test-types | ✅ | check:root-types | ✅ | | |

全 17 项绿 ⇒ 等价于 `npm run check` 通过(同一组步骤)。

## 二、测试套件 ✅

| 步骤 | 结果 | 明细 |
|---|---|---|
| `npm run test:host` | ✅ exit 0 | tests 759 / pass 758 / fail 0 / skipped 1(36.5s) |
| `npm run test:ui` | ✅ exit 0 | 75 文件 / 329 tests 全通过(vitest) |
| `npm run eval`(replay) | ⚠️ exit 1 | 见下「环境性阻断」 |
| `eval` 契约 dry-run | ✅ exit 0 | `KCW_EVAL_CONTRACT_EXECUTOR=1`,28/28 任务通过(100%),证明 scorers/tasks/report 迁移后可用 |

## 三、证据型 Smoke / E2E ✅(均写出 `reports/*.json`)

| 步骤 | 结果 | 证据 |
|---|---|---|
| smoke:e2e(dry-run 离线) | ✅ | `reports/e2e-smoke/e2e-smoke-2026-06-02T03-02-07-558Z.json`(toolCalls 4 / approvals 2 / fileWritten / done) |
| smoke:host | ✅ | 本地文件操作 3 项 applied,审计落盘 |
| smoke:ui | ✅ | UI 契约 smoke,产物+审计落盘 |
| smoke:windows-resources | ✅ | 资源包渲染/交互(file:// 预览模式),无布局 issue |
| smoke:offline-local | ✅ | `reports/offline-local/offline-local-smoke-2026-06-02T03-02-15-515Z.json` |
| smoke:runtime-update | ✅ | `reports/runtime-dependencies/runtime-update-preservation-2026-06-02T03-02-16-309Z.json` |
| smoke:installed-tauri -DryRun | ✅ | `reports/windows-client-smoke/installed-tauri-smoke-20260602T030216Z.json` |
| bench | ✅ | `reports/bench/bench-2026-06-02T03-02-19-538Z.json`(warned [] / failed []) |

## 四、构建完整性 ✅

| 步骤 | 结果 |
|---|---|
| build:resources(tsc → resources/*.js) | ✅ exit 0 |
| build:host-source | ✅ exit 0 |
| build:ui(vite) | ✅ exit 0,143 模块,产物 → `apps/windows-client/ui-dist` |

## 五、重构质量审计(附加视角)

- **JS↔TS 边界**:`clean` — 允许的 JS 是封闭集合(2 个 `.mjs` 引导壳 + 26 个 `resources/*.js` 且与 tsc 产物逐字节一致);无 `.js` 影子源;tsc 产物 `.js` 全部 gitignore 未入库。
- **TS 工程覆盖**:`clean` — 765 手写源全部被某 tsconfig 覆盖;排除项合理。
- **上帝类行为保持**:`clean` — server / agent-runner / memory-store / App 迁移**逐字保持语义**(控制流、默认值如 maxSteps=6、路由/状态码、SQL 含租户隔离与 upsert、SSE 帧、审批高危豁免、全部中文文案一致)。
- **类型安全侵蚀**:`concerns`(非阻断)— `apps/host/src`、`scripts`、`eval`、`ui` 生产代码**零 `any`/零 `@ts-ignore`**;问题集中在 legacy 浏览器包 `apps/windows-client/resources-src/`(162 处 `: any`,机械式转换),但有注释标注为"过渡、待退役后移除"。

## 六、环境性阻断(与代码失败分列,符合 release-checklist 规范)

- **eval replay**:需 `KCW_EVAL_REPLAY_RECORDS` 指向模型录制文件;仓库未提交该输入。`.github/workflows/ci.yml` 不跑 eval;`scripts/verify-mvp.ts` 在缺 records 时主动跳过 eval。→ 非缺陷,缺输入。
- **原"待验收"补齐情况**(详见 §八):真实 Kimi 模型路径 ✅ **已验收**(`smoke:kimi-api` 真实回复)、已安装 exe 实际启动 ✅ **已验收**(`smoke:installed-tauri` 非 `-DryRun` 真实启动)、真实浏览器渲染 ✅ **已验收**(`smoke:rendered-ui` 真实 Edge headless 渲染)。**三项待验收已全部补齐,无遗留。**

## 七、建议(非阻断,可顺手清理)及处理结果

1. ✅ **已删除** `apps/host/dbg-del.ts`(68 行一次性调试脚本,无人引用)。删除后发现它是深度1 `apps/host/*.ts` lint 作用域的唯一占位文件,连带清理了:`tsconfig.host-test.json` 的登记、`package.json` 的 `lint:ts` glob、`eslint.config.ts` 的 `typedSourceFiles`(移除已空的 `apps/host/*.ts` 作用域)。复跑 `npm run check` 17/17 + `build:host-source` 全绿确认。
2. ❌ **撤回(误判)**:原建议把 `tsconfig.host-checkjs.json` 的手维护 `files` 改 glob。实测 `scripts/check-host-types.ts:13` 调 `assertHostTypeCoverage()`,**已强制显式 `files` 必须逐个覆盖全部 host src**——新增 src 文件不登记则 `check:host-types` 直接红。即"静默漏过"的守卫早已存在;显式列表是**有意设计**,改 glob 反而破坏该门禁,已还原。
3. ✅ **已修** `app-run-events.ts:114` 的 `let sep: any` → `number`(类型注解,emit 不变)。`resources-src/` 其余 161 处 `any` 仍按原结论"随该资源路径退役时一并清理"。

## 八、复验与待验收补齐(2026-06-02T07:32–07:36 UTC,当前最终工作区)

### 8.1 完整权威门禁重跑(16 步:15 绿 + eval replay 环境阻断)

在最终工作区(含全部配置清理)**串行**重跑,逐步独立捕获退出码:

| 步骤 | RC | 步骤 | RC | 步骤 | RC |
|---|---|---|---|---|---|
| check(=17 静态门禁,exit 0 ⇒ 全绿) | ✅0 | smoke:e2e(dry-run) | ✅0 | smoke:runtime-update | ✅0 |
| test:host(=check+759 测试) | ✅0(pass 758/skip 1) | smoke:host | ✅0 | smoke:installed-tauri(DryRun) | ✅0 |
| test:ui | ✅0(75 文件/329 测试) | smoke:ui | ✅0 | bench | ✅0 |
| eval(replay) | ⚠️1(同 §六,输入缺失) | smoke:windows-resources | ✅0 | build:resources/host-source/ui | ✅0×3 |
| eval 契约 dry-run | ✅0(28/28) | smoke:offline-local | ✅0 | | |

e2e-smoke 新鲜证据:`toolCallCount 4 / approvals 2 / fileWritten / done / errors []`。原始日志:`build/_verify-logs/run2/*.log`(gitignored);证据产物时间戳 `…2026-06-02T07-32-…`。

### 8.2 真实 Kimi 模型回复 ✅(原待验收)

`node --env-file=.env … smoke:kimi-api` 真实调用 `api.moonshot.cn/v1` / `kimi-k2.6`:`ok=true`、`provider=kimi-api`、`durationMs≈47.4s`、返回真实中文回复(结构化"三条整理建议")并持久化 run 记录 `run_20260602073513_bd48484a`。证据:`reports/kimi-api-smoke/kimi-api-smoke-20260602T073600Z.json`。

### 8.3 已安装 exe 真实启动 ✅(原待验收)

`smoke:installed-tauri`(**非 `-DryRun`**)对已安装 `Agent Cowork v0.2.0`(`%LOCALAPPDATA%\Agent Cowork`):真实启动桌面进程(pid 38456)+主窗口、sidecar `/health`(127.0.0.1:3017)`ok`、guest/tenant auth 往返、SQLite 写链(memory fact 持久化)、runtime deps(sqlite v3.51.2 available、python-embedded 3.12.10 configured)、**重启后 auth/memory/run/schedule 持久化全通过**、退出后 sidecar 正确停止。证据:`reports/windows-client-smoke/installed-tauri-smoke-20260602T073633Z.json`。
> 说明:验证的是**已发布安装产物 v0.2.0**(5-27 构建)的真实运行;当前分支源码态由 §八.1 的 build 三件套覆盖。若需"用当前源码重新构建安装包→重装→再启",属更重的单独操作,本轮未做。

### 8.4 真实浏览器渲染 ✅(原待验收)

`smoke:rendered-ui` 用真实 **Edge headless**(`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`)+ 本机 server(`uiDist:false`,渲染服务端内置 resources 浏览器应用、model 全 mock)渲染并截图:
- **桌面布局(1536×900)**:`title=Agent Cowork`、status「本地 Agent 就绪」,greeting/modeTabs/sidebarActions/quickActions/interactionStream/runCards 七区块齐备,无横向溢出(`width=clientWidth=1536`)。
- **紧凑布局(1366×768)**:`issues: []`。
- **交互全流程在真实浏览器跑通**:plan→approve→execute(afterApprove「已在本机执行 / 已审批 / `doneClass=true`」)、文件上传 + 对话(`runCardCount=2`)、产物 + 审计落盘。
- 该路径正是 `resources-src` 浏览器应用(含本轮 `app-run-events.ts` SSE「执行动态」帧渲染),即重构改动**在真实浏览器中获验**。

证据:`reports/rendered-ui/rendered-ui-smoke-20260602T075421Z.json` + 截图同目录 `…-1536x900.png`。

---
*方法:首轮——并行 Workflow 跑 17 门禁 + 4 审计(只读),串行后台跑测试/smoke/构建。复验(§八)——同一套门禁在最终工作区串行重跑(端口 3017、写报告产物需串行),外加两项真实环境 smoke。原始日志见 `build/_verify-logs/*.log`、`build/_verify-logs/run2/*.log`(均 gitignored)。*
