# Agent Cowork 当前验收档位与差距清单

> 配套文件:《面试演示与上线预备验收标准》。本文件按该标准对**当前状态**做自评。
> 证据来源:2026-06-09 本次会话实测;代码基线 commit `24bd72ad`(工作树仍为 dirty)。
> 关键说明:下面大量条目是 **"未验收(无证据)"而非"已失败"**——验收战役尚未真正跑过。
> 因此当前 `Not Ready` 多数指"证据未采集",少数指"已知红"。两者在本表中分开标注。

---

## 当前档位:Not Ready

判定依据(命中标准 §3 一票否决 / §4 Demo 硬门槛未满足):

- 演示候选树 **dirty**(§3):工作区约 226 个改动/未跟踪文件(已提交 9 个 refine 文件后仍剩)。
- 安装版 smoke **当前失败**(§4/§8):`smoke:installed-tauri` 报 422。
- 13+ 条核心旅程(§5.1–§5.16)**绝大多数无验收证据**(§7「无证据的完成声明无效」)。
- 更新入口可能以 **placeholder endpoint** 暴露(§3,待确认 UI 是否暴露)。

> Not Ready ≠ 全坏。已能跑通且有证据的见下"已确认通过";未跑的见"未验收清单"。

---

## 已确认通过(有本次实测证据)

| 项 | 证据 | 判定 |
| --- | --- | --- |
| 提示优化(refine)端到端 | 安装版运行态 `POST /api/prompt/refine`(guest token)对"把登录那块的报错处理一下"返回 `changed=true` 真改写;host `/health`=200;commit `24bd72ad`;安装目录二进制与构建产物哈希一致 | Pass |
| supersede / 跨轮承接(逻辑层) | 独立复验:`ORION-7→NEBULA-9` 旧值入 `entity_state_history`、当前为新值;`VERIFY-CARRY-77` 第2轮召回到第1轮口令 | Pass(逻辑),安装版 §5.16 全矩阵未验 |
| 类型/构建局部门禁 | `check:root-types`、`check:ui-types`、改动文件 `eslint` 全绿;`prompt-refiner`+`composer-logic` 测试绿;`build:host`/`build:ui`/`cargo tauri build` 成功 | Pass(局部) |

---

## P0(Demo 前必须清零,已知红)

| 项 | 证据 | 标准条目 | 处理 |
| --- | --- | --- | --- |
| 演示从 dirty 树、非 tag 构建 | git 约 226 dirty | §3 | 其余改动 commit/stash → 打 tag → 从该 tag 重新 build+安装后再演示 |
| `smoke:installed-tauri` 422 | 报告 `error: 422`;根因=smoke 用空 `files` 跑 `meeting-actions`,而该 recipe 已 fail-fast(产品行为**正确**,是 smoke 写错) | §4/§8 | 修 smoke:给真实 source,或断言改为期望 422 |
| 更新入口或为 placeholder | `tauri.conf.json` updater endpoint = `updates.agent-cowork.local`(假域名) | §3/§5.10 | 确认 UI 是否暴露"检查更新";若暴露→隐藏或标注"未上线" |
| 核心 P0 旅程无证据 | §5.3 对话承接、§5.16 MASE 均标 P0,但无验收记录 | §7 | 跑 §9 演示脚本 ×3 并按 §7 留证(至少 5.1/5.3/5.4/5.5/5.6/5.16) |

---

## P1(Demo Ready 前清零)

| 项 | 证据 | 标准条目 | 处理 |
| --- | --- | --- | --- |
| 全量 `npm run check` / `npm test` 未端到端验证 | 本次只跑了 root-types/ui-types/局部 lint + 局部测试,17 步 check 链与 host+ui 全量测试未整跑 | §4 | 整跑一次并留证;失败项归类 |
| 安装版核心旅程未走查 | 见"未验收清单" | §4/§5 | 验收战役 |

---

## P2(记录,可延期)

| 项 | 证据 | 标准条目 | 处理 |
| --- | --- | --- | --- |
| `npm audit` 默认源 404 | `npm config get registry` = `registry.npmmirror.com`(无 audit API);`plan/04` 依赖 `npm audit` | §6 | 审计时固定官方 registry,或 CI 内覆盖 |
| 受限沙箱 `spawn EPERM` | `test:ui` / `smoke:rendered-ui` 普通沙箱失败、提权后全过 | §4(允许环境失败+替代证据) | 验收时在提权/正常 Windows 跑并注明 |
| UI 仍 "Kimi" 话术 vs 多 provider | `Composer.tsx:140`、`App.tsx:138` 硬编码"Kimi";配置层已多 provider | §3(文档/能力一致)/§10 | 文案改中性或按 provider 动态 |
| 文档 MSI vs 主线 NSIS | `docs/安装与使用说明.md:13` 提 `.msi 企业部署`;实际产物为 NSIS | §3(文档冲突) | 文档与产物对齐 |

---

## P3 / 已知边界(by design)

| 项 | 证据 | 处理 |
| --- | --- | --- |
| guest 登录公开 + loopback | 单机桌面合理,但本机其它进程可打 loopback;不适合多人/远程 | 文档明记"仅本机桌面",不宣称局域网/多人安全 |

---

## 未验收清单(无证据,需跑验收战役才能定档)

按标准 §5 逐条,当前判定均为 **Not Verified**(不得当作 Pass):

- §5.1 首次启动与 host 生命周期(端口占用/启动失败/退出清理)
- §5.2 provider 切换(空/错 key、错 baseUrl、fallback;key 不回显/不入日志)
- §5.3 对话承接与历史恢复 **(P0)**
- §5.4 文件上传真实性(声称上传成功而实际失败、模型实收文件可追踪)
- §5.5 执行/流式/取消(停止后后台真停)
- §5.6 审批/apply/rollback(无假成功、重试不重复副作用)
- §5.7 工作区与 path jail(中文/长路径、junction、越权)
- §5.8 recipe 与产物(注:`meeting-actions` 空源 422 已知)
- §5.9 定时任务(prompt-only 是否真实现 **待确认**,不可显示为可成功)
- §5.10 运行时依赖/连接器/更新(plan-only vs 真执行、placeholder 更新)
- §5.11 可观测/日志/脱敏
- §5.12 可访问性/键盘/缩放
- §5.13 性能/资源/降级(**且阈值未定义**,见下)
- §5.14 AI 红队与工具安全(注入不得提升为系统指令)
- §5.15 供应链/安装器/发布来源
- §5.16 MASE 全矩阵(崩溃安全 WAL、窗口作用域等)

---

## 距 Release Ready 的结构性缺口(非 Demo 阻塞)

- 安装包签名:`NotSigned`(installer + installed exe,报告实证)。
- updater endpoint:placeholder。
- 尚未建立:SBOM/依赖清单归档、license 合规、漏洞接收处置流程、完整 WCAG 2.2 检查、性能实测报告、红队 eval 自动化。

---

## 标准本身待补(来自评审)

- **性能阈值未定义**:§5.13/§6 要求"目标阈值+实测值",但全文无具体数字 → 当前不可证伪,需补(冷启动/首 token/取消响应/30min 内存增幅 等)。
- **Release 段对单人面试备战过重**:建议在 §5 每条标注「Demo 必过 / Release 才需」,§9 演示脚本即最小子集。

---

## 最短到 Demo Ready 路径(建议顺序)

1. 清理工作树:把非 refine 的改动 commit/stash,打 tag,从 tag 重新 `build:host`+`build:ui`+`cargo tauri build`+安装。
2. 修 `smoke:installed-tauri`(给 `meeting-actions` 真实 source 或断言改 422),让安装版 smoke 绿。
3. 确认并处理更新入口(隐藏/标注 placeholder)。
4. 整跑 `npm run check` + `npm test` 留证(EPERM 用提权环境)。
5. 定义 §5.13 性能阈值。
6. 按 §9 演示脚本连续 3 轮跑通并留证,重点补 §5.3 / §5.16(含一次 prompt injection 必拒、一次强杀重启后 MASE 仍召回)。
