# Copy to `.env` in this directory. Use a random value of at least 32 characters.
# This sample is intentionally not a production credential.
ONLYOFFICE_JWT_SECRET=sample_replace_with_random_32_char_secret
ONLYOFFICE_PORT=8082
